// Display
let W = window.innerWidth;
let H = window.innerHeight;
let X_PAD = W/8;
let Y_PAD = H/8;

// Environmental Constants
let num_of_layers = 10;
let num_of_agents = 10; // per layer

// Algorithm Constants
let eps = 0.1;

